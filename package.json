{
  "name": "velar",
  "private": true,
  "description": "Workspace root for @velar-dev/cli, @velar-dev/rules, and @velar-dev/shared.",
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=20"
  }
}
